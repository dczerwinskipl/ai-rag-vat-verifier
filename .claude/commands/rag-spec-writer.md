---
description: Research RAG architecture options and produce a specification document, architecture diagrams, and implementation plan
argument-hint: "<RAG topic or goal — e.g. 'add generation step to category matcher' or 'naive RAG endpoint for document Q&A'>"
---

You are running the RAG Specification Writer workflow.

Load and follow all instruction files listed in `instructions/agents/rag-spec-writer.agent.instructions.md`.
Then execute the workflow defined in `instructions/workflows/rag-spec-writer-flow.instructions.md`.

RAG topic or goal: $ARGUMENTS

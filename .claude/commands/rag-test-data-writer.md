---
description: Generate realistic Polish invoice test data — adds category seed entries and evaluation cases covering happy path and edge cases, presents for review before writing
argument-hint: "[optional: expansion goal — e.g. 'add pharmaceutical cases' or 'expand edge cases for ambiguous items']"
---

You are running the RAG Test Data Writer workflow.

Load and follow all instruction files listed in `instructions/agents/rag-test-data-writer.agent.instructions.md`.

Expansion goal (optional): $ARGUMENTS
